apt:
  packages:
    - build-essential
    - git
    - make
    - rake
    - tig
    - wget
dirs:
  autojump: "{{ global.dirs.home }}/autojump"
  dotfiles:
    src: /Vagrant/dotfiles
    dest: "{{ global.dirs.dotfiles }}"
